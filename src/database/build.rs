fn main() {
    // Modified from upstream: the world database is no longer embedded at
    // compile time — it is loaded from an external file at runtime (see
    // loaders/compiled.rs), so there is nothing to track here.
}
